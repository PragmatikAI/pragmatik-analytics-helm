nameOverride: ""

url: "pragmatik.co"

collector:
  replicas: 1
  kafka:
    goodTopicName: sp-collector-payloads
    badTopicName: sp-collector-bad-payloads
  serviceType: NodePort
  ingress:
    className: ""
    tls:
      secretName: ""
    annotations:
      kubernetes.io/ingress.class: "nginx"
      nginx.ingress.kubernetes.io/proxy-body-size: "0"
  customPaths: 
    "/customdtsp/tp2": "/com.snowplowanalytics.snowplow/tp2"

enrich:
  salt: "empty"
  kafka:
   inputTopicName: sp-collector-payloads
   enrichedTopicName: sp-enriched
   badTopicName: sp-bad
  enrichment_ua_parser: 
    enabled: true
  enrichment_yauaa:
    enabled: true
  enrichment_referrer_parser:
    enabled: true
  enrichment_event_fingerprint:
    enabled: true
  enrichment_pii:
    enabled: true
  enrichment_campaigns:
    enabled: true

  enrichment_javascript: |
    {
      "key": "value"
    }
  enrichment_api_request: |
    {
      "key": "value"
    }
  enrichment_spiders_and_bots: |
    {
      "key": "value"
    }

iglu:
  apiKey: "REPLACE-ME"
  database:
    host: "snowplow-postgres.snowplow"
    name: "iglu"
    user: "iglu"
    password: "iglu"
    masterUser: "postgres"
    masterPassword: "REPLACEME"
    instanceCount: 2
    image:
      name: ghcr.io/cloudnative-pg/postgresql
      tag: 16.3
    storage:
      pgData:
        sizeGb: 8
      wal:
        sizeGb: 8
    backup:
      schedule: "0 0 1 * * 5"
      name: "iglu-weekly-pg-backup"
      s3:
        backupBucket: ""
        region: ""
        keyId: ""
        secret: ""
        appDb: snowplow-iglu

transformer:
  kafka:
    bootstrapServiceName: kafka
    bootstrapServicePort: 9092
  outputS3:
    path: s3://snowplow-transformer-output
    region: us-east-1
    # secret key
    # accessKeyId: ""
    # access key
    # secretAccessKey: ""
  outputQueue:
    topicName: sp-enriched-transformed
  inputQueue:
    topicName: sp-enriched

