apiVersion: v1
kind: Secret
metadata:
  name: transformer-aws-creds
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  access_key_id: {{ .Values.transformer.aws.accessKeyId | b64enc }}
  secret_access_key: {{ .Values.transformer.aws.secretAccessKey | b64enc }}

